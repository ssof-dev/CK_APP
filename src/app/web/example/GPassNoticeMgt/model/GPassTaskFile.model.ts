export class GPassTaskFileModel {
    accCd		    : string;	// 업무구분
    accKey		    : string;	// 키 (가맹점코드)
    fileNm          : string;   // 파일명 (원본)
    fileUuid		: string;	// 파일명 (UUID)
    filePath		: string;	// 서버저장파일경로
    fileSize		: string;	// 파일사이즈
    fileExt 		: string;	// 파일확장자
    useYn    		: string;	// 사용구분
    rgstDt    		: string;	// 등록일
    rgstId    		: string;	// 등록ID	
    edtrDt    		: string;	// 수정일	
    edtrId    		: string;	// 수정ID	
}

